# ---------------------------------------------------------------------------
# Core MLOps / Kedro stack  (Python 3.9 - 3.13)
# Exact, reproducible versions are frozen in requirements-lock.txt after install
# ---------------------------------------------------------------------------

# Orchestration & project framework
kedro>=0.19.10,<0.20
kedro-datasets[pandas-csvdataset,pandas-parquetdataset,json-jsondataset,pickle-pickledataset]>=4.1.0

# Data & numerics
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0
scipy>=1.12.0

# Modelling
scikit-learn>=1.5.0

# Experiment tracking & model registry
mlflow>=2.16.0

# Explainability
shap>=0.46.0

# Drift monitoring (optional at runtime – pipeline degrades gracefully if absent)
evidently>=0.4.30

# Plotting / reporting
matplotlib>=3.8.0

# Model serving
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# Testing & quality
pytest>=8.0.0
pytest-cov>=5.0.0
