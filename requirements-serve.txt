# Minimal runtime needed to *serve* the trained model (used by the Docker image).
# Keeping this slim makes the production container small and fast to build.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
scikit-learn>=1.5.0
pandas>=2.2.0
numpy>=1.26.0
